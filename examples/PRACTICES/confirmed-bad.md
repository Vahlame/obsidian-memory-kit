# Confirmed anti-patterns

Patterns the user has confirmed they reject. When one reappears, **flag it (ask, don't impose)**
and link the relevant line.

<!-- example -->

- Secrets hardcoded in source (use env vars / a secrets manager).
- `git push --force` without `--force-with-lease`.
