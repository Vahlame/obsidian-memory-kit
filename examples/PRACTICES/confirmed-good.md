# Confirmed good practices

Patterns the user has confirmed they prefer. Reinforce them when they apply (don't nag).

<!-- example -->

- Parameterized queries / prepared statements at every DB boundary.
- Types at module boundaries (no `any` crossing a public API).
